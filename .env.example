# Where hire-us inquiries go: any endpoint that accepts a JSON POST
# (Slack or Discord incoming webhook, Zapier, Make, n8n, a CRM).
# Unset: development logs inquiries; production refuses them.
CONTACT_WEBHOOK_URL=
